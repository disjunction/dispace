var cc = require('cc');

/**
 * opts:
 */
var MoverBuilder = cc.Class.extend({
    ctor: function(opts) {
        this.opts = opts;
    },

    makeMoverByAssembly: function(assembly) {
    }
});

module.exports = MoverBuilder;
