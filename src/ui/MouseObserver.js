var cc = require('cc');

var MouseObserver = cc.Class.extend({
    /**
     * opts:
     * * viewport
     * * interactor
     * @param opts object
     */
    ctor: function(opts) { 
        this.opts = opts;
    },
    
    
});